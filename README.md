# weatherdashboard
This project provides a simple weather ETL pipeline and a weather viewer. It fetches current weather data for major European cities using the Open‑Meteo API, stores it in a SQLite database, and allows users to view the latest weather
